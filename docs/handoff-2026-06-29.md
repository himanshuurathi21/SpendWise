# Handoff — Complete refactoring + full Playwright E2E coverage of every feature

## Goal
Complete all remaining refactoring (P2 items), then write comprehensive Playwright E2E tests covering EVERY feature, button, flow, and edge case across the entire app. Cycle: analyze → refactor → recheck (lint+typecheck+test+build) → fix → recheck.

## State
- **Branch**: master (2b78401)
- **Uncommitted**: Yes — all Phase 0-1 refactoring changes (do NOT commit unless asked)
- **Last action**: Completed all P0 + P1 phases. All verifications pass: 0 lint errors, 0 type errors, 281/281 unit tests, build passes, 0 npm vulns.

## What's already done
- `window.__SW_STORE` dev-guarded
- `categories.ts` deleted (90% duplicate of mockData.ts)
- CSP meta tag added to vite.config.ts
- `EXCHANGE_RATE_API_KEY` VITE\_ prefix removed
- TaxReport innerHTML → safe DOM createElement API
- All 10 npm vulnerabilities fixed
- 4 merchant maps consolidated into `src/data/categoryMap.ts`
- BankSyncView (634→~200 lines) split into SyncingOverlay, SyncResultReview, ONDCConnectView
- CustomCategoriesModal (568→~180 lines) split into CategoryListView, CategoryEditorView, CategoryReassignView
- advisor.ts 154-line if/else chain → 6 topic handlers in `topics/` dir
- eslint-disable removed from 31 files
- Duplicate `sharedModals/` directory deleted
- Non-null assertion + `.filter().map()` → `.flatMap()` fixed

## Decisions made
- `window.__SW_STORE` guarded with `import.meta.env.DEV` at `src/store/index.ts:441`
- Data layers use `Record<string, any>` for API responses (pragmatic, external data shapes)
- `categoryMap.ts` is the single source of truth for merchant→category mapping
- TaxReport PDF generation uses createElement/textContent/appendChild

## Remaining work

### P2: Quality fixes (2-3 hours)
- **P2-1**: Inject cross-feature deps instead of 5 direct imports (DashboardView, ProfileView, RecurringView, SubscriptionManager, SyncDashboard)
- **P2-2**: Add React.memo to ScoreGauge, LoanEligibilityCards, LevelProgress, NotifRow, SkeletonLoader + useCallback to BankSyncView inline handlers

### P3: Full Playwright E2E coverage (8-12 hours)
Every feature, button, flow, edge case must have a test. Work cycle per feature: write test → run → find failures → fix code → re-run.

**Test checklist:**
- Auth/Onboarding: login, validation, family setup, skip flow
- Dashboard: balance, empty state, stat cards, recent transactions, scroll
- Transactions: add form (all fields, validation), quick add/magic input, voice/receipt buttons, list, edit, delete, search, export
- Budget: create, progress bars, edit limit, delete, alerts
- Goals: create, progress update, edit, delete, mark achieved
- Analytics/Reports: charts, category breakdown, credit health, tax report with inputs + PDF + share
- AI: advisor chat, suggestions, spending personality, proactive nudge
- Gamification: level/XP, badges (locked vs earned), quests, savings challenges, leaderboard
- Settings: dark mode, privacy, currency, export/import, reset, profile, notifications
- Navigation: tab switch, back nav, deep links, quick add from any view
- Edge cases: empty states, error states, loading states, large amounts, zero amounts, special chars, overflow

**Tools:** Playwright with `hydrateAfterGoto` pattern, seed via `window.__SW_STORE.addTransaction()`, force state update for zustand v5+React 19 re-render bug.

## Open questions
- None currently — full plan in `docs/plans/refactoring-plan.md`

## Next steps (concrete first action)
```bash
# 1. Read the full updated plan
cat docs/plans/refactoring-plan.md

# 2. Run current E2E to establish baseline
npm run test:e2e

# 3. Start with P2-1 or P2-2, run lint+typecheck+test after each
# 4. Then P3: read existing e2e files, map gaps, write missing tests
```

## References
- `docs/plans/refactoring-plan.md` — full plan with all phases and test matrix
- `tests/e2e/` — 5 existing Playwright test files
- `src/store/index.ts:441` — `__SW_STORE` dev guard
- `src/data/categoryMap.ts` — consolidated merchant category data
- `src/features/analytics/insights/topics/` — extracted advisor topic handlers
- `src/components/layout/CategoryEditorView.tsx` — extracted from CustomCategoriesModal
- `src/features/sync/components/SyncResultReview.tsx` — extracted from BankSyncView

## Suggested skills
build
